# Dockerfile.generator
FROM python:3.8-slim

WORKDIR /app

# Install Python dependencies
RUN pip install --no-cache-dir faker pandas

# Copy data generator script
COPY src/data_generator.py /app/

# Run data generator
CMD ["python", "data_generator.py"]